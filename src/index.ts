// Public API — exports will be added as bounded contexts are implemented.
// NOTE: adding an export here also requires a corresponding "." entry in the
// package.json "exports" field, otherwise the export is unreachable to consumers.